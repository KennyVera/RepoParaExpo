# URL de conexión: reemplaza 'tu_base_de_datos' por el nombre real que creaste
#spring.datasource.url=jdbc:postgresql://localhost:5432/appweb2025b
spring.datasource.url=jdbc:postgresql://localhost:5432/prueba002
spring.datasource.username=postgres

#spring.datasource.password=1234
spring.datasource.password=postgreAdmin19

# Driver de PostgreSQL
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate: 'update' crea las tablas automaticamente si no existen,
# SE DEJA CREATE POR SI ALGO FALLA CON LA Q YA EXISTE
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Configuracion del servidor de correo (Ejemplo para Gmail)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=kespanav@uteq.edu.ec
spring.mail.password=bqdv qvjn wjza ruot
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true